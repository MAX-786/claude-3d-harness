[pytest]
# library/ holds skill scripts that are meant to run inside Blender or on a user's images. Collecting one would import it, which runs it.
testpaths = tests
norecursedirs = library output .cache .git
